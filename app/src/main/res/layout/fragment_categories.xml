<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.vad.budgets.ui.category.CategoriesViewModel" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.category.CategoriesFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/categories"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="@{viewModel.categories == null || viewModel.categories.size() == 0 ? View.GONE : View.VISIBLE}"
            tools:itemCount="6"
            tools:listitem="@layout/item_category"
            tools:visibility="visible" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="@{viewModel.categories == null || viewModel.categories.size() == 0 ? View.VISIBLE : View.GONE}"
            android:layout_gravity="center"
            android:layout_margin="@dimen/large_margin"
            android:text="@string/no_categories_message"
            tools:visibility="gone" />
    </FrameLayout>
</layout>